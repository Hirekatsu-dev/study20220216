version: "3"
services:
  api:
    build: ./src
    command: /bin/sh -c "rm -f api/tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    tty: true
    stdin_open: true
    ports:
      - "3000:3000"
    volumes:
      - ./src:/api